{
  "name": "homework-files",
  "version": "1.0.0",
  "description": "Using the CSV file provided, answer the following questions by reading the file using sequences, not the entire file at once.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --watch app.js"
  },
  "author": "MIlica Gareski",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^3.0.0"
  }
}
